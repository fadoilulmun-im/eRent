{"content":{"type":"API","structure":"","server_on_load":"async ({\n  template,\n  params,\n  render,\n  db,\n  req,\n  reply,\n  user,\n  log,\n  ext,\n  isDev,\n}: Server) => {\n  if (user.user.role != \"guest\") {\n    return reply.status(409).send({\n      status: \"ERROR\",\n      message: \"You're Already Logged In, Please Logout First\",\n      data: user.user,\n    });\n  }\n\n  const { email, password } = req.body;\n  const customer = await db.customer.findUnique({\n    where: {\n      email: email,\n    },\n  });\n\n  if (!customer) {\n    return reply.status(404).send({\n      status: \"ERROR\",\n      message: \"Akun dengan email yang dimasukkan tidak terdaftar\",\n    });\n  }\n\n  const pass_verify = await ext.argon2.verify(customer.password, password);\n\n  if (!pass_verify) {\n    return reply.status(403).send({\n      status: \"ERROR\",\n      message: \"Password yang anda masukkan salah\",\n    });\n  }\n\n  if (!customer.status) {\n    return reply.status(403).send({\n      status: \"ERROR\",\n      message: \"Akun ini masih dalam pengecekan admin\",\n    });\n  }\n\n  user.user = { role: \"customer\", customer };\n\n  reply.status(200).send({\n    status: \"SUCCESS\",\n    message: \"Berhasil login\",\n    data: user.user,\n  })\n}","figma":{}},"title":"login-customer","type":"cms-template","lang":"","status":"SYSTEM","parent_id":"54643","slug":"/api/customer/login","site":"*","id":"40675"}